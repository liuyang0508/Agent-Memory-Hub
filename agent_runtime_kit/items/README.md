# items/ — 占位目录

**实际用户数据不在这里**。

用户的 memory items 默认在 `~/.agent-memory-hub/items/`。
如果你想用其他位置，设置环境变量 `BRAIN_DIR=/path/to/your/brain` 后跑工具。

本目录原本误把代码和数据混在一起（v0.2 早期），v0.2.1 已纠正。
保留此 README 防止误以为 items 应该写在项目目录里。
